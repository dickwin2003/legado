package io.legado.app.ui.web

import android.app.Application
import io.legado.app.base.BaseViewModel

class WebViewViewModel(application: Application) : BaseViewModel(application) {
    // 简单的ViewModel，暂时不需要特殊逻辑
}
